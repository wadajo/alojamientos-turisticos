logging.level.org.springframework=DEBUG
spring.mvc.log-request-details=true

# only used to bootstrap the application, not used in the tests
turismomadrid.mongodb.local=mongodb://localhost:9999/dummy

# Wiremock Cloud URL where the stubs are configured
turismomadrid.endpoint.url=https://dmgve.wiremockapi.cloud/turismomadrid